<resources>

    <!-- 自定义对话框样式 -->
    <style name="dialog_setting" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!-- 自定义对话框中间显示样式 -->
    <style name="dialog_setting_edt" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!-- 自定义对话框底部显示样式 -->
    <style name="transparentFrameWindowStyle" parent="android:style/Theme.Dialog">
        <item name="android:windowBackground">@drawable/piclib_photo_choose_bg</item>
    </style>

    <!-- 自定义对话框底部弹出样式 -->
    <style name="main_menu_animstyle">
        <item name="android:windowEnterAnimation">@anim/piclib_photo_dialog_in_anim</item>
        <item name="android:windowExitAnimation">@anim/piclib_photo_dialog_out_anim</item>
    </style>
</resources>
